const sceneStyles = {
  width: '100vw',
  height: '100vh',
};

const canvasStyles = {
  background: '#fff',
  width: '100vw',
  height: '100vh',
};

type Position = [number, number, number];

export { sceneStyles, canvasStyles };
export type { Position };